-- vft.nvim plugin loader
-- This file is sourced automatically by Neovim's plugin system.
-- It does NOT call setup() - that's left to the user via require("vft").setup().
-- This file only ensures the plugin doesn't load twice.

if vim.g.loaded_vft then
  return
end
vim.g.loaded_vft = true
